contract LockWithHeight(sender, recipient: Program, expirationHeight: Integer) locks valueAmount of valueAsset {
  clause redeem() {
    verify below(expirationHeight)
    lock valueAmount of valueAsset with recipient
  }
  clause default() {
    verify above(expirationHeight)
    lock valueAmount of valueAsset with sender
  }
}